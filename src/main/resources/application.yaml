# HTTP Server
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

#Server info
info:
  app:
    name: ${spring.application.name}
    description: Nursing SAS Ope info API
    groupId: com.torresj
    artifactId: nursing-sas-ope-info
    version: ${APP_VERSION:SNAPSHOT}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nursing_sas_ope_info.fullname" . }}
  labels:
    {{- include "nursing_sas_ope_info.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "nursing_sas_ope_info.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nursing_sas_ope_info-pvc
spec:
  storageClassName: nfs-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi

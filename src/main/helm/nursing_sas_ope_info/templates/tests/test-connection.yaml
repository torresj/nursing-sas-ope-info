apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nursing_sas_ope_info.fullname" . }}-test-connection"
  labels:
    {{- include "nursing_sas_ope_info.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nursing_sas_ope_info.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
